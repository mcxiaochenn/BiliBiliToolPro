info:
  name: passport.bilibili.com
  type: folder
  seq: 1

request:
  scripts:
    - type: before-request
      code: |-
        // Shared sign utility — see lib/sign.js
        const { signRequest } = require('./lib/sign');
        signRequest(req, bru);
